html 
    head 
        title javascript 
        link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css")
    body.container
        h2 Login 
        p Please Login to Continue 
        form(name="loginForm",method='POST').form-group
            label(for="username") username
            input(type="text" name="username",id="username",placeholder="Username").form-control
            label(for="password") Password
            input(name="password",id="password",placeholder="password").form-control
            br
            input(type="submit" label="login").btn.btn-success

        p Don't have an account?
        p Register 
            a(href="/register") here